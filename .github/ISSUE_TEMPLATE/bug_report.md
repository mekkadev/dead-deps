---
name: Bug report
about: Something crashed, parsed wrong, or behaved differently from the docs
title: ''
labels: ['bug']
---

<!--
Reporting a package that was wrongly flagged? Please use the false-positive
template instead — it feeds the calibration corpus.
-->

**What happened**

**What you expected**

**Reproduction**

<!--
The exact command, and the lockfile format involved (package-lock v1/v2/v3,
pnpm-lock v5/v6/v9, yarn.lock classic or Berry, or bare package.json).
A minimal lockfile that reproduces it is the fastest possible fix.
-->

```console
$ dead-deps
```

**Output**

<!-- Re-run with DEAD_DEPS_DEBUG=1 to include the stack trace. -->

```
```

**Environment**

- `dead-deps --version`:
- `node --version`:
- OS:
